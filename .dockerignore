# Version control
.git
.gitignore

# The extension is not part of the server image
extension
dist

# Docs and build tooling (entrypoint is copied explicitly in the Dockerfile)
README.md

# Local certificates / secrets — never bake these into the image
certs
*.pem

# Local/editor cruft
node_modules
npm-debug.log
*.log
.DS_Store

# CI
.github
